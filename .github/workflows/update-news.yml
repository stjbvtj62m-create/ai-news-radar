name: Update AI News Snapshot

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */1 * * *" # 每小时运行一次，避免过于频繁导致 API 超限

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      # 1. 下载仓库代码
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. 安装 Python 环境
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 3. 安装依赖
      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      # 4. 运行抓取脚本
      - name: Run Update Script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          FOLLOW_OPML_B64: ${{ secrets.FOLLOW_OPML_B64 }}
        run: |
          python scripts/update_news.py --output-dir data --window-hours 24

      # 5. 提交更新到 GitHub (这一步保证网页内容会变)
      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          git commit -m "Auto update news data [skip ci]" || echo "No changes to commit"
          git push

      # 6. 【新增】推送消息到飞书
      - name: Feishu Notification
        if: success() && secrets.FEISHU_WEBHOOK != ''
        run: |
          curl -X POST -H "Content-Type: application/json" \
          -d '{
            "msg_type": "text",
            "content": {
              "text": "📢 AI 新闻雷达已更新！\n🔗 访问地址: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
            }
          }' \
          ${{ secrets.FEISHU_WEBHOOK }}

      # 7. 【新增】推送消息到邮箱
      - name: Send Mail Notification
        if: success() && secrets.EMAIL_USER != ''
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.EMAIL_HOST }}
          server_port: ${{ secrets.EMAIL_PORT }}
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "AI News Radar 自动更新成功"
          to: ${{ secrets.EMAIL_RECEIVER }}
          from: "AI News Radar <${{ secrets.EMAIL_USER }}>"
          body: "AI 新闻雷达已成功更新。点击查看最新资讯: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
